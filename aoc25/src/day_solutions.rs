pub fn run_day_part(day: usize, part: u8, input: String) {
    match (day, part) {
        _ => {
            eprintln!("Day {} part {} is not (yet) implemented.", day, part);
            std::process::exit(1);
        }
    }
}
